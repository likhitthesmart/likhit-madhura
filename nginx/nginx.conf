upstream web_upstream { server web:3000; }
upstream api_upstream { server api:4000; }

server {
    listen 80;
    server_name _;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;
    client_max_body_size 10m;

    # API — /api/foo → api:4000/foo (trailing slash strips the prefix)
    location /api/ {
        proxy_pass http://api_upstream/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Immutable build assets
    location /_next/static/ {
        proxy_pass http://web_upstream;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Generated media (video/images)
    location /media/ {
        proxy_pass http://web_upstream;
        add_header Cache-Control "public, max-age=604800";
    }

    location / {
        proxy_pass http://web_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ── SSL: after `certbot certonly` (or your CA), mount certs and enable ──
# server {
#     listen 443 ssl http2;
#     server_name madhuraorganics.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     # ...same locations as above...
# }
